<UserControl
    x:Class="WinRequest.Controls.KeyValueTable"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Column Headers -->
        <Grid Padding="0,2,0,4" ColumnSpacing="6" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="36" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" Text="Key" FontSize="12" FontWeight="SemiBold"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Margin="4,0" />
            <TextBlock Grid.Column="2" Text="Value" FontSize="12" FontWeight="SemiBold"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Margin="4,0" />
        </Grid>

        <!-- Rows -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="RowsPanel" Spacing="3" />
        </ScrollViewer>

        <!-- Add Button -->
        <Button Grid.Row="2" Click="AddRow_Click" HorizontalAlignment="Stretch"
                Margin="0,4,0,0" Padding="4,5"
                Style="{ThemeResource DateTimePickerFlyoutButtonStyle}">
            <StackPanel Orientation="Horizontal" Spacing="4" HorizontalAlignment="Center">
                <FontIcon Glyph="&#xE710;" FontSize="11" />
                <TextBlock Text="添加" FontSize="12" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
